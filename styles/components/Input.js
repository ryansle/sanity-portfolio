const Input = {
  variants: {
    dark: () => ({
      bg: 'white',
      field: {
        color: 'black',
        _placeholder: {
          color: 'gray',
        }
      }
    })
  }
};

export default Input;
